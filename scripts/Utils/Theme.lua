-- ============================================================================
-- Theme - 全局色板
-- Project Smith
--
-- 统一管理所有 Screen 的配色，便于整体调整。
-- 各 Screen 通过 local C = Theme.Colors 引用基础色板，
-- 页面专属色可在 Screen 内扩展：C.mySpecial = { r, g, b, a }
-- ============================================================================

local Theme = {}

-- ============================================================================
-- 基础色板（水墨风 - 提亮版）
--
-- 设计思路：
--   背景用深灰蓝而非纯黑，与暗色水墨面板素材形成层次
--   文字/强调色提亮，确保在深色面板上清晰可读
-- ============================================================================

Theme.Colors = {
    -- 背景层次
    bgPrimary     = { 38,  38,  58,  255 },   -- 主背景：深灰蓝（原 26,26,46）
    bgSecondary   = { 32,  42,  72,  255 },   -- 次背景：藏青（原 22,33,62）
    bgCard        = { 45,  55,  85,  230 },   -- 卡片底：钴蓝灰（原 30,40,68）
    bgCardHover   = { 55,  65,  98,  255 },   -- 卡片悬浮
    bgCardLocked  = { 30,  35,  55,  200 },   -- 卡片锁定

    -- 强调色
    accent        = { 240, 80,  100, 255 },   -- 炉火红（提亮）
    gold          = { 228, 185, 130, 255 },   -- 鎏金（提亮）
    success       = { 95,  215, 205, 255 },   -- 青铜绿（提亮）
    warning       = { 255, 225, 80,  255 },   -- 淬火黄（提亮）

    -- 文字
    textPrimary   = { 245, 240, 230, 255 },   -- 正文：近白（原 232,224,208）
    textSecondary = { 180, 168, 148, 255 },   -- 次要文字（原 160,147,125）
    textLocked    = { 95,  95,  115, 180 },   -- 锁定文字

    -- 按钮
    btnPrimary    = { 195, 135, 70,  255 },   -- 主按钮（鎏金偏深）
    btnHover      = { 215, 155, 80,  255 },   -- 主按钮悬浮
    btnPressed    = { 165, 110, 55,  255 },   -- 主按钮按下
    btnDisabled   = { 90,  90,  110, 255 },   -- 禁用按钮
    btnAccept     = { 85,  178, 140, 255 },   -- 接受按钮（绿）
    btnAcceptHover = { 100, 195, 155, 255 },
    btnUpgrade    = { 95,  215, 205, 255 },   -- 升级按钮（同 success）
    btnBack       = { 70,  60,  90,  255 },   -- 返回按钮
    btnBackHover  = { 90,  80,  110, 255 },
    btnBackPress  = { 55,  45,  70,  255 },

    -- 分隔线
    divider       = { 75,  75,  108, 255 },   -- 分隔线（提亮）

    -- 对话/叙事专用
    bgDialogue    = { 18,  18,  32,  230 },   -- 对话底板（半透明深色）
    bgChoice      = { 50,  60,  92,  240 },   -- 选项底色
    bgChoiceHover = { 65,  75,  112, 255 },
    bgChoicePress = { 38,  48,  72,  255 },
    textNarrator  = { 195, 205, 215, 255 },   -- 旁白文字

    -- 品质/等级标签
    maxTag        = { 228, 185, 130, 200 },   -- 满级标签（同 gold 带透明）

    -- 订单难度
    tierColors    = {
        [1] = { 195, 195, 195, 255 },   -- T1 灰白
        [2] = { 135, 210, 135, 255 },   -- T2 绿
    },
}

return Theme
